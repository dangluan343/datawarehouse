CREATE TABLE IF NOT EXIST public.radar (
    longitude VARCHAR,
    latitude VARCHAR,
    altitude VARCHAR,
    reflectivity VARCHAR,
    total_power VARCHAR,
    velocity VARCHAR,
    spectrum_width VARCHAR,
    time VARCHAR
    );